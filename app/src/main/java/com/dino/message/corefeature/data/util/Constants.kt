package com.dino.message.corefeature.data.util

object Constants {

    const val SEC_MESSAGE_SHARED_PREFERENCES_NAME = "sec_message"

    /**
     * Key for the token in shared preferences.
     */
    const val TOKEN_KEY = "TOKEN"

    /**
     * Key for the refresh token in shared preferences.
     */
    const val REFRESH_TOKEN_KEY = "REFRESH_TOKEN"
}